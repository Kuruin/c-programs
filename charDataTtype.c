#include<stdio.h>
int main () {
    // null character is /0
    // always use '' stings 
    /*Character set used for c is ASCII
    8 bits is 1 byte*/
    char myChar
    printf("Enter a character");
    scanf("%c", &myChar);
    printf("%c", myChar);
    return 0;
}