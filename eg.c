#include <stdio.h>
        #include<conio.h>
        int main() {
            int slices =12;
            double halfSlices;
            halfSlices=(double)slices/2;
            printf("Half pizza slices are : %f",halfSlices);
            
            return 0;
        }